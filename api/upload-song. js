export default function handler(req, res) {
  if (req.method === 'POST') {
    const { fileUrl, title, artist } = req.body;
    res.status(200).json({
      title,
      artist,
      fileUrl,
      approved: false
    });
  } else {
    res.status(405).json({ error: 'Method not allowed' });
  }
}
